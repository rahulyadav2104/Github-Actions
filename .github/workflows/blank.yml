name: hello word
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps: 
      - name: hello step
        run: |
          echo "hello 1"
  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: testphase
        run: echo "test phase"
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy phase
        run: echo " deploy phase"
